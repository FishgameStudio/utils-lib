[build-system]
requires = ["setuptools>=61.0", "cmake", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "utils-lib"
version = "1.0.0"
description = "C++ extension utils for Python"
